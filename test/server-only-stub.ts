// Test stub for Next.js `server-only`. The real package throws when imported
// outside a React Server Component. Vitest runs in a plain Node environment, so
// modules that mark themselves server-only (e.g. Prisma adapters) are aliased to
// this empty module during tests.
export {};
